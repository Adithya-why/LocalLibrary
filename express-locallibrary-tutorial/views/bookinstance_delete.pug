extends layout

block content

  h1 #{title}: #{instance.name}
  

  
    p Do you really want to delete this instance?

    form(method='POST')
      div.form-group
        input#authorid.form-control(type='hidden', name='instanceid', value=instance._id )

      button.btn.btn-primary(type='submit') Delete

      
